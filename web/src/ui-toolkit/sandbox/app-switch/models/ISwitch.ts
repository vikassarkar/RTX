export interface ISwitchProps {
    theme: string;
    customTheme: string;
    onText: string;
    offText: string;
    isChecked: boolean;
    eventChange: any;
}

export interface ISwitchStates {
}