export interface ISwitchProps {
    theme: string;
    customTheme: string;
    onText: string;
    offText: string;
    defaultChecked: boolean;
    eventChange: any;
    data: string;
}

export interface ISwitchStates {
}