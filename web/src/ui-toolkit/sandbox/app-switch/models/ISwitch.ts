export interface ISwitchProps {
    theme: string;
    customTheme: string;
    onText: string;
    offText: string;
    defaultChecked: boolean;
    eventChange: any;
    name: string;
}

export interface ISwitchStates {
}