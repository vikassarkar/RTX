export interface ISwitchProps {
    theme: string;
    customTheme: string;
    onText: string;
    offText: string;
    isChecked: boolean;
    eventChange: any;
    name: string;
}

export interface ISwitchStates {
}